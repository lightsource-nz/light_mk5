# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "freetype-sys"
version = "0.23.0"
authors = ["Coeuvre <coeuvre@gmail.com>"]
build = "build.rs"
links = "freetype"
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Low level binding for FreeType font library"
homepage = "https://github.com/PistonDevelopers/freetype-sys"
readme = "README.md"
keywords = [
    "freetype",
    "ffi",
]
license = "MIT"
repository = "https://github.com/PistonDevelopers/freetype-sys.git"

[lib]
name = "freetype_sys"
path = "src/lib.rs"

[dependencies.libz-sys]
version = "1.1.20"

[build-dependencies.cc]
version = "1"

[build-dependencies.pkg-config]
version = "0.3.11"

[features]
bundled = []

#   vendored third-party code. This lint fires 14 times, from #[derive(Hash, PartialEq, Eq)] on
# structs with function-pointer fields -- upstream noise nobody here will fix. A [patch.crates-io]
# path override is NOT cap-lints'd the way a registry dependency is, so without this every build
# (firmware, editor, host tests) repeats the warning. Silence it at the source so the build is quiet
# everywhere, not filtered per-script.
[lints.rust]
unpredictable_function_pointer_comparisons = "allow"
