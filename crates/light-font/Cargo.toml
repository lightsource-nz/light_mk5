[package]
name = "light-font"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the LGF bitmap font format: a no_std zero-copy reader, and an encoder behind the `alloc` feature"

[features]
default = []
#   the encoder needs Vec; the firmware reader does not. crush turns this on
alloc = []

[dependencies]
