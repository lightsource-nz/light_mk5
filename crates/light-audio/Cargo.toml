[package]
name = "light-audio"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Audio codec drivers for the light framework, over the I2C bus trait"

[dependencies]
light-core = { path = "../light-core" }
