[package]
name = "light_dictaphone_core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "the dictaphone's engine: WAV capture and playback over a FAT card, the event vocabulary, the console, and the display-driving machinery -- everything but the page layout, which a UI crate supplies"

[dependencies]
light-core = { path = "../light-core" }
light-draw = { path = "../light-draw" }
light-display = { path = "../light-display" }
light-font = { path = "../light-font" }
light-input = { path = "../light-input" }
light-ui = { path = "../light-ui" }
light_ui_components = { path = "../light_ui_components" }
light-fs = { path = "../light-fs" }
light-audio = { path = "../light-audio" }
